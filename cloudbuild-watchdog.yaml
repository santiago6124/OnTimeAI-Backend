steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=Dockerfile.watchdog
      - --tag=gcr.io/$PROJECT_ID/ontimeai-watchdog:latest
      - .

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - gcr.io/$PROJECT_ID/ontimeai-watchdog:latest
