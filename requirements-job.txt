# Dependencias del Cloud Run Job (live_pull + live_job).
#
# Separadas de requirements-api.txt porque el job no sirve HTTP y arrastraba
# fastapi, uvicorn[standard] y python-jose[cryptography] sin usarlos. Esas tres
# traen cryptography, pydantic-core, uvloop, httptools y watchfiles, todo peso
# muerto en una imagen que se baja entera en cada ciclo.
#
# Medido el 15/09: un job que solo importa las dependencias y carga el modelo
# tarda 120 s, de los cuales 9 son los imports y ~111 la descarga de la imagen
# y el arranque del contenedor. Ver issue #4.
#
# Si el job empieza a necesitar algo de la API, va aca explicitamente. No se
# vuelve a apuntar a requirements-api.txt.

lightgbm==4.5.0
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
shap>=0.44              # live_pull persiste los valores SHAP por prediccion
joblib>=1.3
requests>=2.28
pyarrow>=14.0           # training_store publica parquet
holidays>=0.46          # ontimeai.features
airportsdata>=20240101  # feature_engineering_v7.airport_lookup
google-cloud-storage>=2.14
google-auth>=2.28
python-dotenv>=1.0
